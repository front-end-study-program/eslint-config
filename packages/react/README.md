# @sea/eslint-config-react

``` pnpm i @sea/eslint-config-react ```

自定义 vue3+ts 共享 eslint 配置

```json
  // .eslintrc
  {
    "extends": [
      "@sea/eslint-config-react"
    ]
  }
```
