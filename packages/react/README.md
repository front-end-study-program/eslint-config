# @study-sea/eslint-config-react

``` pnpm i @study-sea/eslint-config-react ```

自定义 react+ts 共享 eslint 配置

```json
  // .eslintrc
  {
    "extends": [
      "@study-sea/eslint-config-react"
    ]
  }
```
