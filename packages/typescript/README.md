# @study-sea/eslint-config-ts

``` pnpm i @study-sea/eslint-config-ts ```

自定义 ts 共享 eslint 配置

```json
  // .eslintrc
  {
    "extends": [
      "@study-sea/eslint-config-ts"
    ]
  }
```
