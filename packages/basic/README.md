# @sea/eslint-config-basic

``` pnpm i @sea/eslint-config-basic ```

自定义 vue3+ts 共享 eslint 配置

```json
  // .eslintrc
  {
    "extends": [
      "@sea/eslint-config-basic"
    ]
  }
```
