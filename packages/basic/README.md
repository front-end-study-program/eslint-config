# @study-sea/eslint-config-basic

``` pnpm i @study-sea/eslint-config-basic ```

基础 eslint 规则

```json
  // .eslintrc
  {
    "extends": [
      "@study-sea/eslint-config-basic"
    ]
  }
```
